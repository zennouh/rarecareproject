server {
    listen 80;
    index index.php index.html;
    error_log  /var/log/nginx/error.log;
    access_log /var/log/nginx/access.log;
    root /var/www/html;

    # Gateway Routing
    location /api/patients/ {
        fastcgi_split_path_info ^(.+?\.php)(/.*)$;
        fastcgi_pass patients:9000;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME /var/www/html/public/index.php;
        fastcgi_param PATH_INFO $fastcgi_path_info;
    }

    location /api/maladie/ {
        fastcgi_split_path_info ^(.+?\.php)(/.*)$;
        fastcgi_pass maladie:9000;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME /var/www/html/public/index.php;
        fastcgi_param PATH_INFO $fastcgi_path_info;
    }

    location /api/traitment/ {
        fastcgi_split_path_info ^(.+?\.php)(/.*)$;
        fastcgi_pass traitment:9000;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME /var/www/html/public/index.php;
        fastcgi_param PATH_INFO $fastcgi_path_info;
    }

    location /api/medical-dossier/ {
        fastcgi_split_path_info ^(.+?\.php)(/.*)$;
        fastcgi_pass medical-dossier:9000;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME /var/www/html/public/index.php;
        fastcgi_param PATH_INFO $fastcgi_path_info;
    }

    location /api/autontification/ {
        fastcgi_split_path_info ^(.+?\.php)(/.*)$;
        fastcgi_pass autontification:9000;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME /var/www/html/public/index.php;
        fastcgi_param PATH_INFO $fastcgi_path_info;
    }

    location /api/autorisation/ {
        fastcgi_split_path_info ^(.+?\.php)(/.*)$;
        fastcgi_pass autorisation:9000;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME /var/www/html/public/index.php;
        fastcgi_param PATH_INFO $fastcgi_path_info;
    }
}
